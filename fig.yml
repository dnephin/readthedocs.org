#
#
#

webapp:
    build: .
    environment:
        DJANGO_SETTINGS_MODULE: 'webapp_settings'
    volumes_from:
        - webappconfigs
    ports:
        - 8000

webappconfigs:
    build: dev/webapp_config

database:
    build: dev/database
